ちゃんとしたリポジトリでふ

## 実行方法(Mac)

**↓はシェル立ち上げ時に1回だけやる**

1. `export GEMINI_API_KEY=【APIキー】` (APIキーはDiscordに貼ってある)

2. `export CLASSPATH="bin:lib/*"`

**↓は毎回やる**

3. `javac -d bin $(find . -name '*.java')` (必ずこれにすること!!)

4. `java Main`

## 実行方法(Windows)

あとでかく
